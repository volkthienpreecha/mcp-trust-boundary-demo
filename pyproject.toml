[project]
name = "mcp-trust-boundary-demo"
version = "0.1.0"
description = "Local educational demo of config-driven execution risk in MCP-style clients."
requires-python = ">=3.10"

[tool.pytest.ini_options]
addopts = "-p no:cacheprovider"
testpaths = ["tests"]
